# Source Notes

`app.js` and `styles.css` are loaded directly by the static site under
`figures/proxy_landscape_v9_reviewed_site/`.

The source logic intentionally reads retained `raw_outputs/` files rather than
embedding large data blobs in HTML.
