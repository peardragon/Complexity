# AGENTS.md for 02_dnn/08_mnist

## Scope

These instructions apply only to work under `02_dnn/08_mnist`.

## Working agreement

- Start by reading `README.md`, `00_GLOBAL_GOAL.md`, and the relevant stage `README.md`.
- In GOAL mode, execute stages in numeric order.
- Never modify retained production outputs outside `02_dnn/08_mnist`.
- Reuse earlier code by importing or copying with attribution into `02_dnn/08_mnist/src/`; do not alter old behavior.
- Every stage must produce `REPORT.md`, `run_config_resolved.json`, and `QC_STATUS.json`.
- Stop on hard QC failure and write `STAGE_BLOCKED.md`.
- Do not use image generation. All figures must be generated from measured or computed data.
- Keep claims support-limited. Failed radii are `no_claim`.

## Output style

For each completed stage, print:

1. Files created.
2. Files modified.
3. QC summary.
4. Blocking issues, if any.
5. Exact next command or next stage prompt file.
