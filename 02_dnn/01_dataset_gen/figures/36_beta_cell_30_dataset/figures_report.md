# Dataset generation figures report: 36_beta_cell_30_dataset

이 폴더는 dataset generation stage의 active figure만 보관한다. root `figures/*.png` 중복본은 backup으로 이동했고, run별 하위 폴더가 canonical figure 위치이다.

## 입력 데이터

입력: `02_dnn/01_dataset_gen/raw_outputs/36_beta_cell_30_dataset/dataset_index.csv`와 `02_dnn/01_dataset_gen/raw_outputs/36_beta_cell_30_dataset/raw_datasets/`의 representative dataset payload.

## 그림 설명

- `beta_series_grid.png`: beta 값이 증가할 때 생성된 대표 dataset의 공간 패턴을 격자로 보여주는 overview 그림이다.
- `beta_series_grid_scatter.png`: 대표 dataset의 point cloud를 beta 순서대로 비교해 dataset 생성 결과가 beta series를 따라 어떻게 변하는지 확인하는 그림이다.
