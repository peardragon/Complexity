# Smoke Runs

Use this directory for smoke checks, short exploratory runs, and temporary test beds.

Each run repeats this stage shape under smoke_runs/{run_info}/: config, src, raw_outputs, figures, QC.

Contents here are not promoted final results until explicitly moved into the active output paths with provenance.
